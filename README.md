# DB Postgres Status Monitor

A new Flutter project.

## Build as binary
```sh
flutter build linux --release
```

## Build as .DEB
```sh
flutter_distributor release --name=dev --jobs=release-dev-linux-deb
```